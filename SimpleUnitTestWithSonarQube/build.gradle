
plugins {
  id "org.sonarqube" version "2.5"
}

apply plugin: 'java'
apply plugin: 'jacoco'

repositories {
	mavenCentral()
}

dependencies {
	testCompile("junit:junit:+")
	testCompile("org.easymock:easymock:+")
}
